{
  "scripts": {
    "format": "prettier \"**/*.{js,html}\" --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  },
  "dependencies": {
    "@material-ui/core": "^4.9.14",
    "axios": "^0.19.2"
  }
}
